# ZX7981PD LuCI 短信中心开发计划

日期：2026-07-21

## 1. 总体策略

开发前设置两个门禁：

- 门禁 A1（已通过）：外部手机发送唯一短信，本机在 `ME` 中实时收到；同时监控 `usb0`。
- 门禁 A2（已通过）：本机以 GSM 7-bit 向外部手机发送唯一短信，模块返回成功，用户于 23:03 确认实际收到；`usb0` 无中断。
- 产品范围决策（已完成）：运营商短信无法返回剩余流量数字，因此移除一键流量查询、10010 专用状态机、运营商模板和流量解析，仅保留通用短信收发。
- 门禁 B：完成 `lteat` 短信 ubus 方法的契约测试，确认输入、输出、超时、重复发送和删除语义。

两项通过后才进入 LuCI 实现。若门禁 B 失败，则先开发/替换单一 AT broker，不在不可靠后端上堆 UI。

## 2. 阶段与交付物

### P0：基线与可恢复性（0.5 天）

- 记录系统、模块、USB VID/PID、串口映射、网络地址和已装包。
- 备份 `/etc/config/lte`、`/etc/config/network`、现有 LuCI LTE 文件与包清单。
- 只读记录 MTD/UBI 布局；本阶段不刷机。
- 建立脱敏测试日志格式。

交付物：基线清单、备份校验值、回滚步骤。

### P1：短信闭环与后端契约（1–2 天）

- 快照 `CMGF/CSCS/CPMS/CNMI/CSCA`，确认测试后配置未漂移。
- 分别建立 `SM` 与 `ME` 的消息基线；已确认现有 `get_sms` 只读当前存储会漏掉另一存储。
- 持续监控 `usb0` 地址、默认路由、RX/TX 计数和到公网的 ping。
- 验证新回复、中文 UCS2 和长短信 UDH 重组；明确区分 MO 网络提交、最终送达和业务回复。
- 对 `send_sms` 做 GSM、UCS2、非法号码、超时、忙、SIM 未就绪等契约测试。
- 不做删除测试；删除语义使用专门测试 SIM/测试消息后再验证。

退出条件：普通点对点 MO/MT 成功、`ME/SM` 均可见且数据链路无异常；形成稳定接口契约。否则进入 P1B。

### P1B：AT broker 备选（已于 2026-08-05 触发，3–5 天）

- 已确认 `lteat.get_sms:{}` 无分页/索引参数，目标 SM 读取存在不完整响应；
  当前部署保持 fail-closed，详见 `docs/p1b-sms-broker-design-2026-08-05.md`。
- 停止并替换 `lteat` 的串口所有权，而非与其并发；切换前必须盘点并兼容
  数据面所需的 `lteat` ubus 方法。
- procd 托管，独占 TTY，内部队列和 URC 分发。
- 实现版本化的 `scan_begin/scan_read/scan_end`、受限 AT 操作、状态查询、
  SMS PDU 收发和超时状态机。
- 兼容当前目标模块的能力探测与端口映射配置，但不得把型号或固定 TTY
  暴露给 LuCI/API。
- 对照 `sms_tool` 与 Quectel 手册编写测试，但不直接把 shell CLI 暴露给 LuCI。

### P2：核心库与适配服务（3–4 天）

- 建立 OpenWrt package 骨架和 CI。
- PDU 编解码单元：GSM 7-bit/扩展表、UCS2、地址、时间、DCS、UDH。
- `ME/SM` 双存储枚举、合并、去重与带存储区的稳定消息 ID。
- 长短信聚合、缺段状态和消息指纹。
- `modem.sms` ubus API、发送队列、缓存和幂等键。
- 定义模块无关的后端适配器接口；当前适配器封装 `lteat`，统一能力模型和安全错误码。
- 能力探测不得硬编码 RM500U 或 `/dev/ttyUSB2` 到 UI/API；记录模块厂商、型号、传输方式、短信能力和后端 ID。
- 日志脱敏和 rpcd ACL。
- 通用发送前存储容量检查；容量不足时返回 `STORAGE_FULL`，不得进入不确定发送状态。
- `/usr/bin/modem-smsctl` SSH/JSON CLI、schema 版本、受限写操作与 AI 友好错误码。

交付物：`modem-smsd`/适配包、单元测试、API 文档。

### P3：LuCI 26 前端（2–3 天）

- JavaScript view、menu JSON、ACL 与 i18n。
- 收件箱/发件箱、详情、刷新、容量告警。
- 写短信与编码/分段计数、发送确认和进度。
- 删除二次确认和错误恢复。
- 根据通用 `capabilities` 显示支持状态；页面不得出现 RM500U、固定 TTY 或运营商专用逻辑。
- 默认隐私脱敏、无 `innerHTML` 注入点。

交付物：`luci-app-modem-sms`、中英文翻译、页面截图与可访问性检查。

### P4：设备集成与回归（2–3 天）

- 冷启动、服务重启、模块重插、SIM 不在位/PIN、存储满。
- 短短信、中文、长短信、分段乱序/缺段、服务短号。
- 发送超时与页面重复点击，确认绝不重复提交。
- 100 次读取、20 次受控发送稳定性测试。
- 全程监控 `usb0`、IPv4/IPv6、路由、吞吐和日志。
- 验证 LuCI ACL、CSRF、号码/PDU 注入、敏感数据泄露。
- 验证 SSH CLI 的 `list/get/send/status/summary` JSON schema、`--confirm`、幂等、AI 只读摘要和受限密钥策略。
- 用模拟第二后端执行相同契约和 UI 测试；若获得非 RM500U 实机，再增加真实换模回归。
- 回归 LTE 配置保存、SA/NSA 状态、系统升级后菜单和服务。

### P5：发布与回滚（1 天）

- 生成与目标架构匹配的 APK 包和校验值。
- 先灰度安装，不写 bootloader/分区。
- 安装前后备份配置；记录卸载与恢复命令。
- 发布说明列出私有 `lteat` 依赖、支持模块/固件和已知限制。

## 3. 建议仓库结构

```text
packages/
  modem-smsd/
    Makefile
    files/etc/init.d/modem-smsd
    files/usr/sbin/modem-smsd
    files/usr/share/rpcd/acl.d/modem-sms.json
  luci-app-modem-sms/
    Makefile
    htdocs/luci-static/resources/view/modem/sms.js
    root/usr/share/luci/menu.d/luci-app-modem-sms.json
    root/usr/share/rpcd/acl.d/luci-app-modem-sms.json
    po/zh_Hans/modem-sms.po
tests/
  pdu/
  contract/
  device/
docs/
  api.md
  test-matrix.md
  rollback.md
```

## 4. 测试矩阵

| 类别 | 最少用例 |
|---|---|
| 编码 | GSM 基本表、扩展表、UCS2 中文、emoji 拒绝/代理对策略、8-bit 占位 |
| 长短信 | 2/3 段、乱序、重复段、缺段、8-bit/16-bit reference |
| 号码 | E.164、国内号、10010、空值、超长、控制字符、AT 注入 |
| 状态 | SIM READY/PIN/缺卡、注册/未注册、存储满、modem busy |
| 发送 | 成功、CMS ERROR、120 秒超时、重复点击、页面关闭后查询 |
| 接收 | 新消息 URC、轮询、历史消息、索引复用、时间异常 |
| 网络 | IPv4/IPv6 保持、路由保持、ping、吞吐、模块重连 |
| 安全 | 未登录、低权限、CSRF、RPC 越权、XSS、日志脱敏 |
| 生命周期 | 安装、升级、重启、卸载、配置保留与回滚 |
| 后端兼容 | 当前 `lteat`、模拟第二后端、能力缺失、端口变化、`SMS_UNSUPPORTED` |
| SSH/AI | list/get/summary 只读、send 显式确认、JSON schema、脱敏、命令注入、审计 |

## 5. 开发完成定义

- PRD 的全部 MVP 验收项通过并有证据。
- 代码、API、测试和回滚文档齐全。
- 无并发串口 owner，无自动重复发送，无默认自动删除。
- 固件升级或后端不可用时，UI 能明确降级而不影响 5G 上网。
- 临时 SSH 公钥从设备移除，本地临时私钥和 LuCI session 文件清理完成。
