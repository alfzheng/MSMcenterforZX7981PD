# P16V 与 ZX 网络排查报告

日期：2026-07-23  
时区：Asia/Shanghai  
检查性质：只读诊断  
设备范围：

- P16V：Windows 主力电脑，主机名 `DESKTOP-0DMUD3I`
- ZX：ZX7981PD，OpenWrt 25.12.5

## 1. 排查目标

本次排查用于确认：

1. P16V 是否因安装或运行多个代理软件导致网卡、路由、DNS、系统代理或网络组件混乱。
2. P16V 在不运行本地代理时，为什么通过 ZX 访问不同海外服务的表现不一致。
3. ZX 的 PassWall2 当前配置、运行状态、节点、DNS、IPv6 和访问控制是否存在明显问题。

本次没有修改 P16V 或 ZX 的任何网络、代理、防火墙、DNS、节点或订阅配置。

## 2. 执行摘要

### 2.1 总体结论

P16V 当前网络栈没有被多个代理软件持续接管。当前问题主要集中在 ZX 的 PassWall2 节点出口可用性和 IPv6 代理边界，而不是 P16V 的系统代理、路由或活动 TUN 冲突。

当前 PassWall2 虽然显示 Core 正在运行，节点自身的握手测试也正常，但实际出口只能访问部分海外目标：

| 测试项目 | 结果 |
|---|---:|
| 当前节点握手延迟 | 约 76 ms |
| 百度连接 | 成功，约 320 ms |
| GitHub 连接 | 成功，但约 2185 ms |
| Google 连接 | 失败 |
| Chrome 新建 ChatGPT 页面连接 | 超时 |

因此，“服务已启动”和“节点延迟较低”不能证明该节点对 Google、OpenAI 等目标可用。

### 2.2 风险等级

| 等级 | 问题 |
|---|---|
| 高 | 当前日本节点只能访问部分海外目标，Google 检测失败，ChatGPT 新连接超时 |
| 中 | LAN 获得公网 IPv6，但 PassWall2 未启用 IPv6 透明代理，存在 IPv6 绕行和行为不一致风险 |
| 中 | OpenClash 已安装但缺少内核；目前未运行，但未来误启用可能与 PassWall2 冲突 |
| 低 | P16V 留有 v2rayN/Clash 防火墙许可、停止的 Wintun 驱动和便携软件文件 |
| 低 | PassWall2 规则自动更新未开启，订阅及节点列表存在一定历史冗余 |

## 3. P16V 检查结果

### 3.1 当前接入状态

- 当前通过 Wi-Fi `ZX7891PD` 接入 ZX。
- P16V IPv4 地址：`192.168.88.220/24`
- IPv4 默认网关：`192.168.88.1`
- IPv4 DNS：`192.168.88.1`
- 同时获得 ZX 下发的公网 IPv6 地址、IPv6 默认路由和运营商 IPv6 DNS。
- 当前只有 WLAN 存在 IPv4、IPv6 默认路由。
- 没有第二条活动默认路由，也没有活动的 TUN/VPN 默认路由。

多个 `WLAN 2/3/4` 接口是 Qualcomm FastConnect 6900 的 DBS/辅助 Wi-Fi 接口，当前均未连接，不是代理软件创建的虚拟网卡。

### 3.2 系统代理与 DNS

- WinHTTP：直连，无代理。
- WinINET/Internet Settings：未设置 `ProxyServer`、PAC 或自动配置地址。
- 当前 Web 代理运行时判断为直连。
- 未发现 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY` 等持久环境变量。
- hosts 文件为 Windows 默认内容，没有域名劫持记录。
- 未发现 NRPT 域名解析策略。
- 未发现端口转发规则。

### 3.3 代理软件运行状态

当前未发现以下代理/VPN程序正在运行：

- Clash Party / mihomo
- v2rayN / Xray
- sing-box
- OpenVPN / WireGuard
- Tailscale / ZeroTier
- Proxifier 等

本地监听端口中也没有发现常见代理监听端口或对应进程。

### 3.4 本机残留

发现以下历史残留：

1. Clash Party 便携版文件仍位于下载目录。
2. `D:\tools\v2rayN-windows-64\v2rayN.exe` 仍存在。
3. Windows 防火墙中有两条 v2rayN 入站允许规则。
4. Windows 防火墙中有一条 Clash Party/mihomo 入站允许规则。
5. `wintun.sys` 驱动仍存在，版本 0.14，服务状态为停止、启动模式为手动。

这些项目目前没有活动进程、监听端口、虚拟网卡或默认路由，因此不是当前访问异常的直接原因。后续可以按用户意愿进行清理，但不需要为了本次故障立即重置 Winsock、TCP/IP 或网卡。

### 3.5 关于命令行 HTTPS 测试

Codex 为命令执行环境设置了专用出站阻断规则，因此从诊断子进程执行的 `curl` 超时不能直接代表 P16V 普通应用的真实网络状态。

本报告没有用这些受沙箱影响的结果判断 ZX 故障；实际结论来自：

- P16V 与 ZX 的真实网卡、路由和代理状态；
- Chrome 新建页面的实际表现；
- ZX PassWall2 自带的百度、Google、GitHub连接检测；
- ZX 的实际进程和 PassWall2 运行日志。

## 4. ZX 基础网络状态

### 4.1 系统信息

- 型号：ZX7981PD
- 固件：OpenWrt 25.12.5 `r33051-f5dae5ece4`
- LuCI：26.133
- 内核：6.12.94
- 平台：`mediatek/filogic`

检查时系统负载、内存和存储没有显示资源耗尽。

### 4.2 蜂窝上游

ZX 的上游通过 `usb0` 工作：

- IPv4 地址、网关和运营商 DNS 均存在。
- IPv6 地址、默认网关和运营商 IPv6 DNS 均存在。
- P16V 的 DHCP 租约正常。
- P16V 与 ZX 的 Wi-Fi 信号和链路速率正常。

因此 ZX 的基础蜂窝链路和 LAN/WLAN 接入不是本次问题的主要故障点。

## 5. PassWall2 配置审计

### 5.1 当前运行状态

- PassWall2 主开关：开启
- Core：运行中
- 路由器本机代理：开启
- 客户端代理：开启
- 当前节点：`Xray VLESS：[日本 03]`
- Socks 监听端口：1070
- Socks 仅本机监听：开启
- 独立 Socks 配置：未启用

P16V 没有被排除。由于客户端代理已开启且访问控制未启用，LAN 客户端默认都应进入 PassWall2。

### 5.2 实际进程

ZX 上实际运行的相关进程包括：

- PassWall2 Xray Core
- PassWall2 专用 dnsmasq
- PassWall2 monitor/lease 脚本
- 系统 dnsmasq

没有发现 OpenClash 或 mihomo 进程。因此目前不是 PassWall2 与 OpenClash 同时运行造成的规则冲突。

### 5.3 当前节点与出口

节点列表中：

- 当前日本节点握手测试约 76 ms。
- 两个美国节点握手测试约 97–100 ms。

但当前节点的实际出口测试结果明显不完整：

- 百度正常；
- GitHub可连接但延迟超过 2 秒；
- Google失败；
- P16V Chrome 新建 ChatGPT 页面连接超时。

这说明当前节点可能存在下列情况之一：

1. 节点出口对 Google/OpenAI 有限制；
2. 节点订阅已限流、失效或线路质量下降；
3. 节点到部分 Cloudflare/Google 网络段的路由异常；
4. 当前线路对不同目标的分流结果不一致。

仅凭节点列表中的 76 ms 不能排除这些问题，因为该值通常只反映到节点服务器的连接延迟，不代表节点到最终网站的出口质量。

### 5.4 DNS

当前 PassWall2 DNS 设置：

- DNS 重定向：开启
- 直连查询策略：`UseIP`
- 远程 DNS 协议：TCP
- 远程 DNS：Cloudflare `1.1.1.1`
- 远程 DNS 出站：远程节点
- 远程查询策略：`UseIPv4`
- FakeDNS：关闭
- 域名重写包含常见 DoH 域名到固定 DNS 地址的映射

这套 IPv4 DNS 配置总体合理，可以避免大多数代理域名直接返回 AAAA 记录。

但它不能完全阻止应用自行使用 DoH。浏览器或应用如果通过 DoH 获得 IPv6 地址，仍可能绕开系统 DNS和 PassWall2 的 IPv4代理路径。

### 5.5 IPv6

ZX 正在向 LAN 下发可用的公网 IPv6和默认路由，但 PassWall2 的“IPv6透明代理（TProxy）”未开启。

这会形成：

```text
IPv4：可能经过 PassWall2
IPv6：默认直接通过运营商出口
```

潜在后果：

- 同一个域名的 IPv4和IPv6访问结果不一致；
- 支持 Happy Eyeballs 的应用表现随机；
- 使用 DoH 的浏览器可能绕过 PassWall2；
- 出现 IPv6 泄漏；
- 登录、认证和长连接可能走不同出口 IP，影响会话稳定性。

这是当前配置中最明确的结构性风险。

### 5.6 透明代理与防火墙

- 首选防火墙工具：nftables
- TCP代理方式：REDIRECT
- TCP转发端口：全部
- UDP转发端口：全部
- IPv6 TProxy：关闭
- ICMP 劫持：关闭
- nftables 规则加载日志显示完成

IPv4 的 REDIRECT/TPROXY 组合没有发现明显配置错误。

### 5.7 访问控制

- 访问控制主开关：关闭
- 没有单独的客户端规则

因此 P16V 不存在因 MAC、IP 或 ACL 配置而被排除代理的问题。

### 5.8 订阅与节点

当前存在以下订阅分组：

| 分组 | 已解析节点数 |
|---|---:|
| 4D4Y | 2 |
| FLZT | 36 |
| IGG | 10 |
| IGGnew | 13 |

已知情况：

- 4D4Y 本月流量已经用完，不应继续作为有效出口使用。
- 部分 IGG 节点在订阅解析时因缺少对应二进制而被丢弃。
- `IGG` 与 `IGGnew` 同时存在，可能属于历史和新订阅并存，需要后续确认用途。

这些条目不会自动导致系统故障，但会增加选错失效节点或误判节点来源的可能。

### 5.9 规则

- 存在 China、Iran、Russia、PrivateIP 等分流规则。
- 日志显示 China/geoip 规则成功加入 nftset。
- GeoIP 和 Geosite 被选为更新对象。
- 自动更新规则没有开启。

未发现规则加载失败，但长期关闭自动更新可能导致分流资源逐渐过期。

### 5.10 OpenClash

OpenClash 已安装并出现在 LuCI 菜单中。打开其页面时弹出“下载内核”提示，说明 OpenClash 当前缺少运行内核。

已关闭提示，没有执行下载。进程检查未发现 OpenClash/mihomo 运行，因此：

- 它不是本次故障的直接原因；
- 目前没有和 PassWall2 同时争抢 nftables 或 DNS；
- 未来若误装内核并启用，可能与 PassWall2 发生严重冲突。

## 6. 根因判断

### 6.1 可以排除

现有证据可以基本排除：

- P16V 系统代理未关闭；
- P16V 存在活动 TUN/VPN；
- P16V 有多条活动默认路由；
- hosts 被代理软件修改；
- WinHTTP 或环境变量残留代理；
- P16V 被 PassWall2 访问控制排除；
- OpenClash 与 PassWall2 当前同时运行；
- ZX 蜂窝上游完全断开；
- PassWall2 Core 完全没有启动。

### 6.2 最可能原因

当前最可能是两个问题叠加：

1. 当前 `日本 03` 节点对 Google/OpenAI 等目标不完全可用，或者该线路已受限、降速。
2. ZX 下发公网 IPv6，但 PassWall2 只代理 IPv4，导致不同应用和不同连接可能走不同出口。

P16V 本地残留属于后续清理项，不是当前根因。

## 7. 建议处置顺序

### P0：先验证替代节点

1. 不再使用已耗尽流量的 4D4Y 节点。
2. 从 FLZT、IGG 或 IGGnew 中选择一个明确可用的节点。
3. 每次只切换一个节点。
4. 切换后依次检测：
   - 百度
   - GitHub
   - Google
   - ChatGPT 新建页面
   - Codex 重新连接或登录
5. 只有全部达到预期后才将其作为当前节点。

切换节点会重载 PassWall2 和 nftables，执行前应保持 LuCI 管理页面可访问。

### P1：统一 IPv6 策略

在以下两种方式中选择一种：

方案 A，暂时关闭 LAN IPv6：

- 适合先追求稳定；
- 所有代理目标只走 IPv4；
- 容易判断问题是否来自 IPv6 旁路。

方案 B，开启 PassWall2 IPv6 TProxy：

- 前提是当前节点明确支持 IPv6；
- 需要同时验证 IPv6 DNS、TCP、UDP、泄漏和重连；
- 风险和调试复杂度高于方案 A。

在节点能力不明确时，建议先采用方案 A 做诊断。

### P2：整理代理软件

ZX：

- 保持 OpenClash 关闭。
- 若确定长期只使用 PassWall2，可以后续备份后卸载 OpenClash，避免误操作。

P16V：

- 可以在确认不再需要后删除 Clash Party 便携目录。
- 可以删除不用的 v2rayN 防火墙许可。
- 可以在确认无软件依赖后卸载 Wintun 驱动。
- 不建议无原因执行 Winsock 重置、TCP/IP 重置或删除所有网卡。

### P3：整理订阅与规则

- 确认 `IGG` 与 `IGGnew` 是否为重复或替代关系。
- 删除已经失效且不会再使用的历史节点。
- 不要使用已耗尽流量的 4D4Y 节点。
- 确认缺失的代理核心是否真的需要；不要为了保留所有订阅节点盲目安装大量二进制。
- 在完成备份后评估启用 GeoIP/Geosite 定期更新。

## 8. 建议的验收标准

修复后应同时满足：

1. P16V 不运行本地代理软件。
2. P16V 只有预期的 ZX 默认路由。
3. PassWall2 Core 正常运行。
4. OpenClash/mihomo 不运行。
5. 百度、GitHub、Google 检测均成功。
6. Chrome 可新建并加载 ChatGPT 页面。
7. Codex 可重新登录，而不只是维持旧长连接。
8. IPv4/IPv6 出口策略明确，不存在非预期 IPv6 绕行。
9. 停止 PassWall2 后能够恢复普通蜂窝 NAT 上网。
10. ZX 重启后节点、DNS、nftables 和蜂窝上游均能按预期恢复。

## 9. 操作记录与安全说明

本次仅执行读取和连通性检测：

- 没有保存或应用任何 LuCI 表单。
- 没有切换节点。
- 没有更新订阅。
- 没有下载 OpenClash 内核。
- 没有启动 OpenClash。
- 没有重启 PassWall2、网络、防火墙或 ZX。
- 没有修改 P16V 网络设置。
- 没有删除 P16V 代理软件或驱动。

报告不包含订阅链接、节点密码、LuCI 会话令牌或其他访问凭据。
