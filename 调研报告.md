# ZX7981PD 短信功能调研报告

更新日期：2026-07-20

产品决策更新（2026-07-21）：由于现场已证明 10010 短信入口不能返回剩余流量数字，产品移除“一键查询流量”及其专用解析、运营商模板和 SSH 查询命令，仅保留通用短信收发、存储管理和诊断能力。下文的 10010 调研记录作为范围决策证据保留，不代表待开发功能。

## 1. 结论摘要

背景文件中的型号与版本需要更正：现场设备是 **ZX7981PD**，不是“ZX789PD/ZX7891PD”；当前固件是 **OpenWrt 25.12.5 r33051-f5dae5ece4**，不是 25.12.4。

短信能力并未在底层缺失。现场的 Quectel RM500U-CNV 模块支持标准 3GPP SMS AT 指令，固件内常驻的 `lteat` 进程也已经注册 `get_sms`、`send_sms`、`del_sms` 等 ubus 方法。当前真正缺失的是可靠、现代且安全的 LuCI 短信产品层，以及对现有私有后端的参数、错误语义和兼容性的验证。

推荐路线：以现有 `lteat` 为唯一串口所有者，开发受限的短信适配服务和 LuCI 26 JavaScript 前端；适配服务必须同时处理 `ME` 与 `SM`，不能只读取当前 `CPMS mem1`。若 `lteat.send_sms` 的接口或稳定性不合格，再进入“替换为开源 AT broker”的备选路线。生产实现不得让多个进程并发打开同一 AT 串口。

## 2. 现场调查

### 2.1 设备与系统

| 项目 | 现场结果 |
|---|---|
| 设备型号 | ZX7981PD |
| SoC/架构 | MediaTek MT7981 / ARMv8 / `mediatek/filogic` |
| 固件 | OpenWrt 25.12.5 r33051-f5dae5ece4 |
| 内核 | Linux 6.12.94 |
| LuCI | 26.133.20346~e9ebca7 |
| 内存 | 约 484 MiB |
| 可写 overlay | 46.5 MiB，总使用约 768 KiB |
| 5G 数据接口 | `usb0`，CDC-NCM，IPv4/IPv6 正常 |
| 运营商/制式 | 中国联通，NR5G-SA，N78（调查时） |

隐私说明：调查读取到了 IMEI、ICCID、IMSI 和号码，但本文不记录完整值。

### 2.2 5G 模块

| 项目 | 现场结果 |
|---|---|
| 模块 | Quectel RM500U-CNV |
| 模块固件 | RM500UCNVAAR03A09M2G_01.001.01.001 |
| USB 串口 | `/dev/ttyUSB0` 至 `/dev/ttyUSB4` |
| 现有 AT 口 | `/dev/ttyUSB2` |
| AT 所有者 | `lteat -d /dev/ttyUSB2 -t quectel-ecm -e` |
| 数据口 | `usb0`，网关 `10.11.95.1` |

模块官方 AT 手册覆盖 `+CSMS`、`+CMGF`、`+CSCA`、`+CPMS`、`+CMGL`、`+CMGR`、`+CMGS`、`+CMGD`、`+CNMI` 等短信命令，支持 PDU/Text、GSM/IRA/UCS2。CDC-NCM 数据链路与 AT 控制口可以共存，但同一 AT 口不应由多个进程并发读写。

### 2.3 现场短信只读验证

通过已有 `lteat` ubus 服务完成，未直接抢占串口，未删除或修改短信：

| 检查 | 结果 |
|---|---|
| SIM 状态 | `READY` |
| SMS service | `+CSMS: 0,1,1,1` |
| 支持模式 | PDU 0、Text 1；当前 PDU 0 |
| 字符集 | GSM、IRA、UCS2；当前 UCS2 |
| 存储 | SIM `SM`，18/50 |
| 短信中心 | 已配置（本文脱敏） |
| 新消息通知 | `+CNMI: 2,1,0,0,0` |
| 列表读取 | `lteat.get_sms` 成功返回 18 条 PDU |
| SIM 历史记录 | 存在发往 10010 的查询记录，以及来自 10010 的中文分段回复；记录位于 SIM 的 `SM` 存储，可能来自此前使用该 SIM 的其他设备 |

### 2.4 2026-07-20 实时收发测试

发送侧测试一：路由器通过 `lteat.send_sms` 向 10010 发送一条 UCS2 编码的 `CXLL`。PDU 为合法的 SMS-SUBMIT，TPDU 长度 18 字节；模块先写入索引 19，再返回 `AT+CMSS=19`、`+CMSS: 1`、`OK`，索引状态为 `STO SENT`。10010 未产生新回复，因此该项只证明模块完成了 MO 网络提交流程，不能证明 10010 最终业务受理。ASCII 指令被编码为 UCS2 是短码业务不回复的高优先级兼容假设，尚需以 GSM 7-bit 的单次测试验证。

发送侧测试二：路由器向刚发来测试短信的手机回复 `ZX7981PD-TX-TEST`，使用 GSM 7-bit 并设置 TP-SRR。模块写入索引 20，返回 `AT+CMSS=20`、`+CMSS: 2`、`OK`，索引状态为 `STO SENT`；用户确认手机于 23:03 实际收到。因此本机普通点对点 MO 发送已完成端到端验证。

10010 修正测试：将 `CXLL` 改为 GSM 7-bit 并请求状态报告，只发送一次。模块写入索引 22；索引 23 的 SMS-STATUS-REPORT 显示 MR 3、目标 10010、23:08:14 最终送达、状态码 0。23:08:15 收到来自 10010 的索引 24/25 两段 UCS2 长短信，UDH 重组成功。回复要求通过中国联通 App 官方链接查询当前流量，没有直接返回剩余流量数值。由此确认 10010 闭环可用，也确认快捷查询必须使用 GSM 7-bit，并处理“数值回复”和“仅链接回复”两种结果。

网上交叉调研：联通官方短信营业厅兼容表除 `CXLL` 外，还列出 `CXYL`/`CXTCYL`（套餐余量）、`CXZLL`（手机上网总流量）以及发送 `10010` 获取当前菜单。该表同时声明业务会更新、以实际办理方式为准，并含大量历史 3G 指令，因此只能作为候选来源，不能保证 2026 年河北 5G 套餐仍返回数值。`KTTX`、`AZTX`、`ATTX`、`QXTX` 和各类开通/退订指令会改变业务状态，未纳入测试。

候选指令已于 2026-07-20 23:41–23:56（北京时间）通过本机逐项实测完成。所有 ASCII 指令均使用 GSM 7-bit，中文自然语言使用 UCS2；全部请求均成功产生 10010 业务回复，测试期间 `usb0` carrier 始终为 1，公网 ping 无丢包。

| 指令 | 官方/候选含义 | 本卡实际结果 |
|---|---|---|
| `CXYL` | 套餐余量 | 最终送达；两段回复，仅 App 链接，无数字 |
| `CXTCYL` | 套餐余量别名 | 最终送达；两段回复，仅 App 链接，无数字 |
| `CXZLL` | 手机上网总流量 | 最终送达；两段回复，仅 App 链接，无数字 |
| `2082` | 套餐余量数字代码 | 最终送达；两段回复，仅 App 链接，无数字 |
| `1071` | 手机上网流量数字代码 | 最终送达；两段回复，仅 App 链接，无数字 |
| `10010` | 获取短信菜单 | 回复“未能理解业务编码”，引导 App，无菜单 |
| `1076` | 总流量数字代码 | 回复“未能理解业务编码”，引导 App |
| `TCYL` | 省分/历史套餐余量别名 | 最终送达；回复“未能理解业务编码” |
| `107` | 流量菜单代码 | 最终送达；回复“未能理解业务编码”，无菜单 |
| `208` | 套餐查询菜单代码 | 最终送达；回复“未能理解业务编码”，无菜单 |
| `查流量` / `查询流量` | 官网所述自然语言入口 | 均最终送达；回复“未能理解业务编码” |

因此，对这张正式河北联通卡，已经没有发现能通过 10010 短信返回权威流量数字的可用指令。可识别的查询代码被网侧统一改为 App 深链，不识别的代码和自然语言则返回 App 引导。继续穷举历史指令的收益很低，并可能触发限频；产品不能再把“短信返回数字”作为必然前提。

测试还暴露出容量门禁：`SM` 上限 50 条，从 25 条基线连续测试后曾占满，导致 `1076` 的送达报告/回复延后，释放存储后回复才写入。已只删除本轮产生的索引，最终恢复到测试前的 25 条；原有索引 1–25 未删除。产品必须在查询前检查可用槽位，并为一条长回复至少预留 4 个槽位。

接收侧：用户于约 22:50 从另一部手机发送唯一内容 `0720test`。现有 `lteat.get_sms` 在 `SM` 中反复读取均看不到新消息；临时仅将读取存储切换到 `ME` 后，在索引 14 找到内容完全匹配的新 SMS-DELIVER。测试后已恢复 `CPMS` 为 `SM/SM/SM`，未删除短信。

因此，本机 **普通点对点 MO/MT 均已端到端成功**；原“收不到短信”的直接系统原因是新消息落在 `ME`，而现有页面/后端只读取 `SM`。`CMGL=4` 还会把未读短信改为已读，因此现接口也不是真正无副作用的只读 API。两次发送/接收调查期间 `usb0` carrier 始终为 1，公网 ping 无丢包，数据链路未中断。

历史记录本身仍不能证明本机能力；但本轮已通过独立的点对点 MO、点对点 MT 和 GSM 7-bit `CXLL -> 10010 -> 两段联通回复` 三项实时测试，当前设备的普通短信与 10010 查询链路均已完成端到端验证。

### 2.5 现有软件栈

- `lteat` 来自 `ledeat-1.0-r20200723`，是无公开源码的预编译程序。
- `lteat` 已在 ubus 注册 `get`、`send`、`get_sms`、`send_sms`、`del_sms`、`send_ussd` 等方法，并持有 `/dev/ttyUSB2`。
- `luci-app-lte` 仅提供 5G 配置和状态页面，没有短信菜单。
- 固件安装了 `comgt`、`fibo-qmi-dial`、`fm350-modem` 等通用/历史兼容包，但现场模块不是 Fibocom FM350。
- `sms_tool` 是可参考的开源实现，但它会直接打开 TTY 且自身不提供全局锁；不能在 `lteat` 运行时直接并用同一串口。

### 2.6 换模兼容性结论

短信能力与模块有关，但 LuCI 产品层不应与 RM500U 绑定。现场实际依赖链是：LuCI/RPC → `lteat` 私有 ubus 接口 → `/dev/ttyUSB2` → RM500U 的 3GPP SMS AT 能力。更换模块后：若现有固件仍通过相同 `lteat` 契约管理短信，只需重新验证；若新模块支持标准 SMS AT 但 `lteat` 不兼容，需要更换后端适配器或 AT broker；若模块只提供 QMI/MBIM 短信，则需要相应协议后端；若没有可访问短信能力，则功能不可用。

因此应把 LuCI、`modem.sms` API、PDU 编解码和安全模型设计为模块无关，把厂商、端口、协议和私有返回格式限制在后端适配器内。不能承诺任意 5G 模块即插即用，但换模不应要求重写前端。

### 2.7 现有 LuCI/LTE 代码问题

1. 使用旧式 Lua controller/CBI 和模板，未采用 LuCI 26 的 JavaScript view、menu JSON 与细粒度 RPC ACL。
2. 状态页直接展示完整 IMEI/ICCID/IMSI，默认隐私策略不合格。
3. 页面用 `innerHTML` 写入运营商/模块返回值，应改为 `textContent` 或可靠转义。
4. 状态解析依赖脆弱的字符串下标，对 SA/NSA、不同模块返回格式和缺字段不稳健。
5. 配置回调存在疑似缺陷：`ip_type` 是字符串却与数值 `0` 比较；命令文本中还出现 `ifup l-w te0` 的可疑拼写。
6. `get_sms` 一次现场调用约需 8–10 秒，不能做高频轮询；需要缓存、超时和请求合并。
7. `lteat` 同时暴露改 IMEI、重置、切网等高风险方法。短信 UI 的 RPC ACL 必须只白名单短信所需方法，不能把整个对象暴露给普通 LuCI 会话。
8. `get_sms` 固定执行 PDU 模式的 `CMGL=4`，只查询当前 `mem1`。现场已证明新短信可进入 `ME` 而页面在读 `SM`，必须合并两个存储并记录来源。
9. `CMGL=4` 会将 `REC UNREAD` 改为 `REC READ`；后端必须明确“刷新会改变未读状态”，或改用 URC/索引级读取策略维护可靠未读语义。

## 3. GitHub 与硬件支持调研

### 3.1 型号确认

GitHub 上能找到 ZX7981PD 的设备树、适配 PR 和问题记录，而 ZX7891PD/ZX789PD 没有对应有效结果。结合现场 `ubus call system board`，可确认背景文件是型号笔误。

### 3.2 OpenWrt 支持边界

- ZX7981PD 的 24.10/25.12 支持主要存在于 ImmortalWrt/社区 MT798x 分支。
- 调研时未见设备进入官方 `openwrt/openwrt` 主线，因此应标注为“社区支持”，不能宣称官方支持。
- 存在 stock 双 UBI 与 ubootmod 单 UBI 等不同分区方案。未备份并确认分区布局前，禁止刷写固件。
- 需要回归的社区问题包括 USB-WAN/HNAT 吞吐、sysupgrade 后 `rax0` 未入 `br-lan`、部分 5 GHz 客户端兼容性。

## 4. 风险与决策

| 风险 | 影响 | 决策/缓解 |
|---|---|---|
| `lteat` 无公开源码 | 参数、错误语义和可维护性不透明 | 已完成初步黑盒契约；适配层隔离；保留可替换后端 |
| 串口并发 | AT 响应串线、数据链路异常、短信误发 | 生产环境保持唯一 AT owner；所有操作排队 |
| `CMGS` 超时 | 重试可能造成重复短信 | 请求 ID、发送状态机；超时后先查状态，不自动重发 |
| 中文/长短信 | 乱码、分段丢失或顺序错误 | 完整支持 UCS2、GSM 7-bit、UDH 拼接与原始 PDU 留存 |
| SIM 存储仅 50 条 | 存满后无法接收 | 容量告警；删除必须人工确认；不默认自动清理 |
| `ME`/`SM` 分裂 | 页面漏掉实际已到达的短信 | 同时枚举并合并两个存储；消息 ID 包含存储区与索引 |
| 10010 回复异步 | 误判发送失败 | MO 成功与 MT 收到分开记录；接收窗口至少 120 秒 |
| 敏感标识泄露 | 隐私/安全风险 | 默认脱敏；仅管理员按需查看；日志禁止写完整号码与 PDU |
| 固件差异 | 升级后串口号/返回格式变化 | 通过 USB VID/PID 与能力探测选择后端，不硬编码单一版本 |

## 5. 资料来源

- [Quectel RM500U 系列产品页](https://www.quectel.com/product/5g-rm500u-series/)
- [Quectel RM500U 系列规格书](https://www.quectel.com/content/uploads/2024/04/Quectel_RM500U_Series_5G_Specification_V1.4.pdf)
- [Quectel RGx00U/RM500U AT Commands Manual](https://quectel.com/content/uploads/2024/02/Quectel_RGx00URM500U_Series_AT_Commands_Manual_V1.0.pdf)
- [中国联通短信营业厅](https://iservice.10010.com/elecService/message.html)
- [ZX7981PD 24.10 适配 PR](https://github.com/padavanonly/immortalwrt-mt798x-6.6/pull/141)
- [ZX7981PD 25.12 适配 PR](https://github.com/chasey-dev/immortalwrt-mt798x-rebase/pull/11)
- [ZX7981PD 25.12 DTS](https://github.com/chasey-dev/immortalwrt-mt798x-rebase/blob/25.12/target/linux/mediatek/dts-ext/mt7981b-wirelesstag-zx7981pd-ubootmod.dts)
- [sms_tool](https://github.com/obsy/sms_tool)
- [5G-Modem-Support](https://github.com/Siriling/5G-Modem-Support)
