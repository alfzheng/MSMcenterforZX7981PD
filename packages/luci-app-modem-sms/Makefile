include $(TOPDIR)/rules.mk

LUCI_TITLE:=LuCI support for the modem SMS service
LUCI_DEPENDS:=+modem-smsd
LUCI_PKGARCH:=all
PKG_VERSION:=0.1.0
PKG_RELEASE:=6

include ../../luci.mk

# call BuildPackage - OpenWrt buildroot signature
