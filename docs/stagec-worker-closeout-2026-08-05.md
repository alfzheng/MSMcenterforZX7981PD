# Stage C worker closeout / handoff — 2026-08-05

## Current verified state

Repository: `D:\Projects\ZX7891PD 优化` on `main`.

Latest owned commits:

```text
d512715 [Codex@gpt-5] Add Stage C fault injection coverage
fdca6a7 [Codex@gpt-5] Bind Stage C jobs to lease acquisition time
6a23073 [Codex@gpt-5] Add fail-closed Stage C worker recovery
```

The only unrelated working-tree item is the pre-existing untracked
`4d4yapi.md`; it has not been edited or committed.

Target read-only state captured before the archive-only rehearsal on 2026-08-05
(r6 baseline):

```text
modem-sms-archived: 0.1.0-r6
archive_enabled=0
archive_copy_enabled=0
archive database absent
stage_c_delete_enabled=false
archive capabilities: ARCHIVE_DISABLED
public delete probe: DEVICE_DELETE_DISABLED
```

No SMS list, read, send or delete operation was performed.

The post-rehearsal final state is recorded separately in
`docs/archive-only-rehearsal-2026-08-05.md`: `modem-sms-archived r12`,
`archive_enabled=0`, `archive_copy_enabled=0`, the archive database preserved,
and the device-delete gate still returning `DEVICE_DELETE_DISABLED`.

## Delivered scope

- Database-only Stage C worker with lease acquire/renew/release.
- Server-side time source with test-only clock injection.
- Durable lease owner, nonce, storage, generation and `acquired_at` binding.
- Bounded recovery at 5,000 pending items/jobs with fail-closed state.
- Restart recovery to `lost`, `unknown` or `blocked`, with immutable audit events.
- Delete-job and delete-item triggers bound to the closed gate and active lease.
- `move_local` protection against entering `deleting`.
- Legacy `stage_jobs` migration before parsing new triggers.
- Startup recovery before archive UBus registration.
- No public Stage C RPC, CLI, LuCI action or modem delete path.

## Verification matrix

Local checks passed:

```text
node --no-warnings tests/stagec-sql.js
node --no-warnings tests/archive-sql.js
node --no-warnings tests/archive-contract.js
powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\static.ps1
node --check tests/stagec-sql.js
git diff --check
```

Target OpenWrt 25.12.5 / aarch64 checks passed with the installed r12
runtime files:

```text
lua tests/stagec-worker.lua
lua tests/stagec-fault-injection.lua
lua tests/archive-migration.lua
lua tests/archive-runtime.lua
```

The fault-injection test covers SQLite lock contention, rollback/no residual
lease, close/reopen recovery, exact recovery events and repeated-recovery
idempotence. Its temporary database path is generated by `os.tmpname()` and
is owned by the test.

## Audit disposition

- Godel’s first independent audit found 3 P1 and 7 P2 findings; all were
  addressed.
- Erdos’ completed re-audit found no P0/P1 and one conditional P2, the missing
  explicit `lease_acquired_at` binding; it was fixed in r6.
- Dewey’s fault-injection review found a P1 test-harness deletion risk and P2
  assertion gaps; those were fixed in `d512715` and the target test passed.
- Halley’s earlier timed-out attempt is retained as incomplete and is not
  counted as a passing audit.

Full evidence is in
`docs/stagec-worker-adversarial-audit-2026-08-04.md`.

## Deployment evidence

Historical Stage C deployment package (r6 baseline; retained for provenance):

```text
modem-sms-archived-0.1.0-r6.apk
SHA-256: 89adb99e530315a804f558e72ac463dc103eaa96288821708b915ee855e9a130
```

The target-side package hash matched before installation. The r6 deployment
backup is retained in the ignored local directory
`.build-temp/deploy-stagec-r6-20260804/`.

```text
sysupgrade-before.tar.gz SHA-256:
6ef8eb37eb449a8d354f6425e284758605ff6f63307058b597c80a1e8df61154
```

Package-index warnings for unavailable third-party indexes did not affect the
local APK transaction, package version, hash match or read-only acceptance.

## Archive-only rehearsal result

The bounded archive-only enablement rehearsal is complete. It used a fresh
target backup, exercised the target BusyBox/LuaSQLite3 runtime, verified the
read-only archive capability/page path, checked negative capacity fail-closed
behavior, and rolled back to the disabled configuration while preserving the
empty archive database. The detailed evidence is in
`docs/archive-only-rehearsal-2026-08-05.md`.

The next gate is a separate review of whether to retain the archive foundation
disabled or authorize a longer observation window. Archive copy, Stage C
destructive work, and device deletion remain out of scope; enabling any delete
capability still requires a separate design review, approval and modem
fault-injection plan.
